package com.littlefox.app.foxschool.adapter.listener

interface DetailItemListener
{
    fun onItemClickThumbnail(index : Int)
    fun onItemClickOption(index : Int)
    fun onItemSelectCount(count : Int)
}